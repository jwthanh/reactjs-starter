import React from "react";
import AppDrawerContainer, { Props } from "./AppDrawerContainer";

const AppDrawer = (props: Props) => {
  return <AppDrawerContainer {...props} />;
};

export default AppDrawer;
